<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/dp_16">

    <ImageView
        android:id="@+id/image_view_empty_state"
        android:layout_width="@dimen/dp_300"
        android:layout_height="@dimen/dp_300"
        android:contentDescription="@string/empty_state_label"
        tools:src="@drawable/drawable_booking_history_empty" />

    <TextView
        android:id="@+id/text_view_empty_state_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dp_16"
        android:textAlignment="center"
        android:textAppearance="?attr/textAppearanceHeadline3"
        android:textColor="?attr/colorOnSurface"
        tools:text="Your history is empty" />

    <TextView
        android:id="@+id/text_view_empty_state_description"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/dp_4"
        android:textAlignment="center"
        android:textAppearance="?attr/textAppearanceBody1"
        android:textColor="?attr/colorTextMedium"
        android:visibility="gone"
        tools:text="Start booking!"
        tools:visibility="visible" />
</LinearLayout>